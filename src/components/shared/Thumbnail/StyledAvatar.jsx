import { styled } from "@mui/material/styles";
import { Avatar } from "@mui/material";

const StyledAvatarComponent = styled(Avatar)(({ theme }) => ({
	borderRadius: 12,
}));

export default StyledAvatarComponent;
